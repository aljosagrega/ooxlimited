# Copy to .env and fill in. .env is gitignored and must stay that way.
# Values come from the hosting control panel: Advanced -> SSH Access.
OOX_SSH_HOST=
OOX_SSH_PORT=65002
OOX_SSH_USER=
OOX_SSH_KEY=$HOME/.ssh/ooxlimited-deploy
OOX_REMOTE_DOCROOT=domains/ooxlimited.com/public_html
